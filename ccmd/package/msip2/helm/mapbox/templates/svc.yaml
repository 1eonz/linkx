apiVersion: v1
kind: Service
metadata:
  name: mapbox
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: mapbox
  ports:
    - name: http
      port: 10101
      protocol: TCP
      targetPort: 10101
  type: ClusterIP