apiVersion: v1
kind: Service
metadata:
  labels:
    name: registry-open
  name: registry-open
  namespace: platform
spec:
  ipFamilies:
    - IPv4
  ports:
    - port: 5001
      protocol: TCP
      targetPort: 5001
  selector:
    app: registry
  type: ClusterIP
  externalIPs:
    - 10.28.15.61
