apiVersion: v1
kind: Service
metadata:
  labels:
    name: redis-open
  name: redis-open
  namespace: platform
spec:
  ipFamilies:
    - IPv4
  ports:
    - port: 6379
      protocol: TCP
      targetPort: 6379
  selector:
    app: redis
  type: ClusterIP
  externalIPs:
    - 10.28.15.61
