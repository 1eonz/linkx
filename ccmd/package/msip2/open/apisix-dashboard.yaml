apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: apisix-dashboard
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: apisix-dashboard
    app.kubernetes.io/version: 2.13.0
    helm.sh/chart: apisix-dashboard-0.6.0
  name: apisix-dashboard-open
  namespace: platform
spec:
  ipFamilies:
    - IPv4
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
      nodePort: 30080
  selector:
    app.kubernetes.io/instance: apisix-dashboard
    app.kubernetes.io/name: apisix-dashboard
  sessionAffinity: None
  type: NodePort