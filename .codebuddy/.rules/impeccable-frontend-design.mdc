---
description: 
alwaysApply: false
enabled: true
updatedAt: 2026-04-17T02:03:28.131Z
provider: 
---

# Impeccable 前端设计审查标准

> 基于 Impeccable: Premium UI Design Expertise (github.com/pbakaus/impeccable)
> 审查维度、反模式约束和审查清单，用于指导前端交互设计质量评审。

---

## 一、7 大审查维度

### 1. Typography（字体系统）
- 使用有特色的字体配对，避免过度常见字体
- 建立清晰的字号层级：标题 / 正文 / 辅助文字比例分明
- 行高控制在 1.4-1.6（正文），1.2-1.3（标题）
- 字重对比：标题 600-700 vs 正文 400，差距至少 200
- 中文场景优先考虑：思源黑体、霞鹜文楷等有特色字体

### 2. Color & Contrast（色彩与对比度）
- 使用 OKLCH 色彩空间进行配色（比 HSL 更均匀的感知）
- 中性色必须带色调（tinted neutrals），禁止纯黑 #000 或纯灰 #808080
- 暗色模式下背景用带色调的深色（如 #1a1a2e 而非 #111111）
- 文字对比度：正文 ≥ 4.5:1（WCAG AA），大标题 ≥ 3:1
- 彩色背景上的文字必须用该色系的浅色/深色变体，禁止直接用灰色
- 语义色：success 绿 / warning 橙 / danger 红 / info 蓝，保持一致

### 3. Spatial Design（空间与布局）
- 使用 4px/8px 基础间距系统（4, 8, 12, 16, 24, 32, 48, 64）
- 内间距 < 外间距（组件内紧凑，组件间宽松）
- 建立视觉层级：标题区 > 内容区 > 辅助区，通过间距和背景区分
- 避免多层嵌套卡片（最多 2 层），优先用间距和分隔线代替卡片嵌套
- 表单 label 对齐方式一致（left / right / top 全局统一）

### 4. Motion Design（动效设计）
- 缓动曲线：优先使用 `cubic-bezier(0.4, 0, 0.2, 1)`（Material 标准）
- 禁止使用过时的 bounce / elastic 缓动曲线
- 动画时长：微交互 150-200ms，过渡 200-300ms，复杂动画 300-500ms
- 列表项使用 staggered animation（交错动画），间隔 50ms
- 尊重 `prefers-reduced-motion`，减少动效偏好下禁用非必要动画
- 动效必须有目的：引导注意力、表达状态变化、提供空间感

### 5. Interaction Design（交互设计）
- 表单：实时验证 + 提交时验证双保险，错误消息紧邻字段
- 焦点状态：所有可交互元素必须有可见的 focus ring
- 加载状态：按钮 loading 态、骨架屏、skeleton、进度条，禁止白屏
- 空状态：提供有意义的插图 + 文案 + 引导操作按钮
- 危险操作（删除、禁用）：必须二次确认，说明不可逆后果
- 批量操作：未选中时按钮 disabled，选中后显示选中数量
- 开关/切换：重要状态变更应有确认，防误触
- 弹窗：`close-on-click-modal=false`，Esc 可关闭，打开时焦点 trap

### 6. Responsive Design（响应式设计）
- 移动优先：先设计小屏再扩展大屏
- 断点：320 / 768 / 1024 / 1440px
- 搜索栏：小屏下折叠为图标，点击展开
- 表格：小屏下转为卡片列表或横向滚动
- 操作按钮：小屏下收入"更多"下拉菜单
- 弹窗：小屏下全屏展示

### 7. UX Writing（UX 文案）
- 按钮标签：动词开头，2-4 字（"保存"而非"确定"，"删除"而非"操作"）
- 错误消息：说明原因 + 建议操作（"网络超时，请检查连接后重试"）
- 空状态：友好 + 引导（"还没有角色，点击新建第一个角色"）
- 确认弹窗：说明后果（"删除后该角色下用户将失去对应权限"）
- 状态文案：简洁明确（"启用成功"而非"操作成功"）
- 禁用态：tooltip 说明原因（"系统内置角色不可删除"）

---

## 二、16 个反模式约束（必须避免）

| # | 反模式 | 正确做法 |
|---|--------|----------|
| 1 | 使用 Arial / Inter / system-ui 作为主字体 | 选择有特色的字体，建立字体配对 |
| 2 | 彩色背景上使用灰色文字 | 使用该色系的浅色/深色变体 |
| 3 | 使用纯黑 #000000 或纯灰 | 使用带色调的中性色（如 #1a1a2e） |
| 4 | 多层嵌套卡片（3层+） | 用间距、分隔线、背景色区分层级 |
| 5 | 使用 bounce / elastic 缓动曲线 | 使用标准缓动 cubic-bezier(0.4, 0, 0.2, 1) |
| 6 | 无焦点状态的交互元素 | 所有可交互元素添加 focus ring |
| 7 | 白屏加载无反馈 | 骨架屏 / loading 态 / 进度条 |
| 8 | 空状态只显示"暂无数据" | 插图 + 友好文案 + 引导操作 |
| 9 | 危险操作无二次确认 | confirm 弹窗 + 后果说明 |
| 10 | 批量按钮始终可点击 | 未选中时 disabled |
| 11 | 表单验证消息远离字段 | 错误消息紧邻对应字段下方 |
| 12 | 操作按钮过多横排 | 收入"更多"下拉，最多 3 个直接展示 |
| 13 | 硬编码文案不走国际化 | 所有用户可见文案走 $t() / i18n |
| 14 | 弹窗点击遮罩关闭 | close-on-click-modal=false |
| 15 | 系统内置项无视觉标识 | 添加"系统"标签 + 禁用态 tooltip |
| 16 | 信息截断无完整查看方式 | 添加 title / tooltip / 点击展开 |

---

## 三、审查清单模板

当执行 /critique 或 /audit 时，按以下清单逐项检查：

### 层级与清晰度 (Hierarchy & Clarity)
- [ ] 页面有明确标题或面包屑
- [ ] 信息层级通过字号/字重/颜色清晰区分
- [ ] 主要操作突出，次要操作弱化
- [ ] 视觉流向引导用户完成核心任务

### 交互完整性 (Interaction Completeness)
- [ ] 所有按钮有 loading / disabled / hover 态
- [ ] 表单有实时验证 + 提交验证
- [ ] 危险操作有确认弹窗
- [ ] 批量操作有选中态控制
- [ ] 状态切换有反馈

### 无障碍 (Accessibility)
- [ ] 对比度符合 WCAG AA (4.5:1)
- [ ] 可交互元素有 focus 状态
- [ ] 图片/图标有 alt / aria-label
- [ ] 弹窗有焦点 trap

### 响应式 (Responsive)
- [ ] 搜索栏小屏适配
- [ ] 表格小屏适配
- [ ] 弹窗小屏适配
- [ ] 操作按钮小屏适配

### 国际化 (i18n)
- [ ] 无硬编码中文/英文文案
- [ ] 文案长度变化不影响布局
- [ ] 日期/数字格式本地化

### 情感共鸣 (Emotional Resonance)
- [ ] 空状态友好有引导
- [ ] 错误状态有建设性建议
- [ ] 成功状态有正向反馈
- [ ] 加载状态降低焦虑（进度/骨架屏）

### 性能感知 (Perceived Performance)
- [ ] 首屏有骨架屏
- [ ] 长列表有虚拟滚动或分页
- [ ] 图片有懒加载
- [ ] 动画不阻塞交互

---

## 四、指导命令参考

当用户使用以下命令时，按对应维度重点审查：

| 命令 | 重点审查维度 |
|------|-------------|
| /audit | 无障碍、性能、响应式 |
| /critique | 层级、清晰度、情感共鸣 |
| /normalize | 色彩、间距、字体对齐设计系统 |
| /polish | 动效、微交互、视觉细节 |
| /simplify | 减少嵌套、精简操作步骤 |
| /clarify | UX 文案、错误消息、引导 |
| /optimize | 加载性能、渲染性能 |
| /harden | 错误处理、边界情况、i18n |
| /animate | 缓动曲线、交错动画、减少动效 |
| /colorize | OKLCH 配色、色调中性色、暗色模式 |
| /bolder | 字重对比、色彩对比、视觉冲击 |
| /quieter | 降低色彩饱和度、减少装饰 |
| /delight | 微动效、空状态插图、成功动画 |
| /extract | 可复用组件抽取、设计 token |
| /adapt | 响应式断点、容器查询 |
| /onboard | 新手引导、空状态引导、tooltip 引导 |