# backup_nacos/manifest.yaml
order: 50002
name: backup_nacos
error_ignore: false
gather_facts: false
handler_host: kube_control_plane
always_exec_upgrade: true
phases:
  - patch_install
models:
  - standalone
  - three_nodes
  - multi_nodes