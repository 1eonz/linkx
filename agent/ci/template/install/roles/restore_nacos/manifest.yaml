# restore_nacos/manifest.yaml
rollback_order: 50355
name: restore_nacos
error_ignore: false
gather_facts: false
handler_host: kube_control_plane
always_exec_rollback: true
phases:
  - patch_rollback
models:
  - standalone
  - three_nodes
  - multi_nodes